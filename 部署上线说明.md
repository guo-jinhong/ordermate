# OrderMate 部署上线说明

更新日期：2026-08-05

## 目标

这个文档用于说明项目如何从本地运行，整理到 GitHub，并部署到一台可公网访问的服务器上。

面试材料里建议最终提供两类地址：

```text
GitHub 仓库地址：https://github.com/你的用户名/ecommerce-order-system-memory
线上演示地址：http://你的服务器IP:18000
```

注意：GitHub 主要负责代码托管，不负责直接运行 Java 后端、MySQL 和 Agent 服务。真正“可访问的项目地址”需要部署到云服务器、Render、Railway、Vercel + 云数据库等运行环境。这个项目包含 MySQL、Java 后端、Python Agent，更推荐先用云服务器 + Docker Compose 部署。

## 一、本地启动方式

本地调试时，在项目根目录运行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-local-full.ps1
```

这个脚本会自动处理：

- 设置 Windows 终端 UTF-8。
- 检查并启动 MySQL。
- 初始化数据库表和演示数据。
- 启动 Java Spring Boot 后端，默认端口 `8080`。
- 启动 Python Agent 服务，默认端口 `18000`。
- 启动后访问本地页面测试。

本地访问地址：

```text
http://localhost:18000
```

停止本地服务：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\stop-local-full.ps1
```

## 二、哪些文件不能提交到 GitHub

一定不能提交真实密钥、数据库密码、JWT 密钥。

不能提交：

```text
.env
agent-service/.env
任何包含真实 OPENAI_API_KEY / DeepSeek API Key 的文件
```

当前 `.gitignore` 已经忽略了 `.env` 和 `agent-service/.env`，但提交前仍建议检查：

```powershell
git status --short
```

如果看到 `.env` 出现在待提交列表里，立刻停止提交。

如果曾经不小心把 DeepSeek Key 提交到 GitHub：

1. 去 DeepSeek 开放平台立刻删除或重置这个 Key。
2. 本地重新生成新的 Key。
3. 清理 Git 历史后再公开仓库。

## 三、本地环境变量配置

本地根目录 `.env` 示例：

```env
MYSQL_ROOT_PASSWORD=123456
JWT_SECRET=local-demo-jwt-secret-key-change-before-production

BACKEND_HOST_PORT=18080
AGENT_HOST_PORT=18000

AGENT_MODE=live
OPENAI_API_KEY=你的DeepSeekKey
OPENAI_MODEL=deepseek-v4-flash
OPENAI_BASE_URL=https://api.deepseek.com

DB_TYPE=mysql
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=123456
MYSQL_DATABASE=ecommerce_db

ECOMMERCE_BACKEND=api
ECOMMERCE_API_BASE_URL=http://localhost:8080/api
```

说明：

- `OPENAI_API_KEY` 实际使用的是 DeepSeek Key。
- 变量名沿用 OpenAI-compatible API 的写法，因为 DeepSeek 接口兼容 OpenAI 调用格式。
- 本地可以写进 `.env`，但 `.env` 不能提交。
- 线上不要使用本地演示密码 `123456`。

## 四、提交到 GitHub

如果当前目录还没有正常 Git 仓库，可以执行：

```powershell
git init
git add .
git commit -m "Initial OrderMate project"
```

在 GitHub 新建空仓库后，把远程地址换成你自己的：

```powershell
git remote add origin https://github.com/你的用户名/ecommerce-order-system-memory.git
git branch -M main
git push -u origin main
```

如果本地已经有 Git 仓库，只需要：

```powershell
git add .
git commit -m "Update OrderMate deployment docs"
git push
```

提交前建议检查是否误提交密钥：

```powershell
git status --short
git diff --cached
```

## 五、服务器部署推荐方案

推荐使用一台 Linux 云服务器，例如阿里云、腾讯云、华为云、AWS Lightsail 等。

服务器需要安装：

- Git
- Docker
- Docker Compose

### 1. 拉取代码

在服务器上运行：

```bash
git clone https://github.com/你的用户名/ecommerce-order-system-memory.git
cd ecommerce-order-system-memory
```

### 2. 创建服务器 `.env`

在服务器项目根目录创建 `.env`：

```env
MYSQL_ROOT_PASSWORD=请换成强密码
JWT_SECRET=请换成至少64位的随机字符串

BACKEND_HOST_PORT=18080
AGENT_HOST_PORT=18000

AGENT_MODE=live
OPENAI_API_KEY=你的DeepSeekKey
OPENAI_MODEL=deepseek-v4-flash
OPENAI_BASE_URL=https://api.deepseek.com

CONVERSATION_MAX_MESSAGES=12
CONVERSATION_TTL_SECONDS=1800
```

线上 `.env` 只保存在服务器，不提交 GitHub。

### 3. 启动服务

服务器项目根目录运行：

```bash
docker compose up -d --build
```

查看服务状态：

```bash
docker compose ps
```

查看日志：

```bash
docker compose logs -f agent
docker compose logs -f backend
docker compose logs -f mysql
```

停止服务：

```bash
docker compose down
```

如果要清空数据库重新初始化：

```bash
docker compose down -v
docker compose up -d --build
```

这个命令会删除 MySQL volume，谨慎使用。

## 六、线上访问地址

Docker Compose 默认暴露：

```text
Agent / 前端：http://服务器IP:18000
Java API：http://服务器IP:18080
MySQL：不建议公网开放
```

安全组需要开放：

```text
18000：前端和 Agent 服务访问
18080：可选，仅调试 Java API 时开放
```

不建议开放：

```text
3306 / 3307：MySQL 数据库端口
```

面试简历里可以写：

```text
项目地址：http://服务器IP:18000
GitHub：https://github.com/你的用户名/ecommerce-order-system-memory
```

## 七、DeepSeek Key 安全规则

必须遵守：

- 不要把真实 Key 写进 README。
- 不要把真实 Key 写进 GitHub 仓库。
- 不要截图暴露完整 Key。
- 不要把 `.env` 上传到网盘或公开仓库。
- 线上服务器只通过环境变量或 `.env` 注入 Key。
- 如果 Key 曾经泄露，马上去 DeepSeek 后台删除并重新生成。

GitHub Actions 如果以后需要用到 Key，应放到：

```text
GitHub 仓库 -> Settings -> Secrets and variables -> Actions -> New repository secret
```

例如：

```text
DEEPSEEK_API_KEY=你的真实Key
```

代码里读取环境变量，不要硬编码。

## 八、上线前检查清单

### 代码检查

```powershell
git status --short
```

确认没有这些文件：

```text
.env
agent-service/.env
*.log
target/
__pycache__/
.pytest_cache/
```

### 本地功能检查

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-local-full.ps1
```

浏览器打开：

```text
http://localhost:18000
```

建议测试：

```text
4000以内手机
2026-07之后发布的耳机
价格100-300的充电宝
把刚才那个红米手机加入购物车
查看我的购物车
把红米手机改成2件
确认
两个商品都改成3件
确认
查询我的订单
```

### Agent 测试

```powershell
cd .\agent-service
.\.venv\Scripts\python.exe -m pytest tests/test_agent.py tests/test_conversation_state.py tests/test_api.py tests/test_ecommerce_client.py -q
```

### Java 编译

```powershell
.\mvnw.cmd -DskipTests compile
```

## 九、简历里怎么写更真实

不要只写：

```text
优化了智能客服功能。
```

建议写成：

```text
基于 DeepSeek Chat Completions 实现电商客服 Function Calling 链路，将商品、价格、库存、购物车、订单等结构化数据查询强制路由到 Java API 和 MySQL，避免模型直接编造业务结果。
```

购物车可以写：

```text
设计 ConversationState 上下文状态，记录最近商品、购物车条目和待确认动作，支持“把刚才那个红米手机加入购物车”“两个商品都改成3件”等多轮指代操作。
```

确认流可以写：

```text
对下单、取消订单、修改购物车等写操作增加确认机制，用户确认后才执行真实 API 调用，降低工具误调用导致的无效写入风险。
```

如果要写量化数据，必须先真实测试。可以补充：

```text
通过固定评测集统计 Function Calling 命中率、工具调用成功率、平均响应时间、购物车操作成功率。
```

不要编造“提升 60%”“减少 80%”这类数字。先用测试脚本或 JMeter 跑出结果，再写到简历。

## 十、建议补充的量化指标

后续可以真实测试并记录：

| 指标 | 测试方式 |
| --- | --- |
| Function Calling 命中率 | 固定 30 条商品/订单/购物车问题，统计是否正确触发工具 |
| 工具调用成功率 | 统计工具执行成功次数 / 总调用次数 |
| 平均响应时间 | 统计聊天接口从发送到最终回答的耗时 |
| 购物车操作成功率 | 加购、改数量、删除、查看购物车各跑 10 次 |
| 幻觉率 | 商品价格、库存、订单回答是否来自真实工具结果 |

简历里只写真实测出来的数据。
