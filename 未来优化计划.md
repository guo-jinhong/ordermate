# OrderMate 电商智能客服 · 未来优化计划

更新日期：2026-08-02

## 当前状态

OrderMate 当前已经进入“Function Calling 可验证落地”阶段。项目保留 Java API 作为业务后端，Agent 服务负责 DeepSeek 调用、工具编排、上下文记忆和确认流，前端负责本地聊天测试。

当前本地链路目标是：

```text
用户问题 -> Agent 意图判断 -> DeepSeek 生成工具调用 -> Python 执行工具 -> Java API/数据库返回真实结果 -> DeepSeek 整理回答 -> 前端展示
```

当前优先级仍然是先把 Function Calling 稳住，再接入 RAG。RAG 只负责售后、保修、规则、FAQ、产品手册等文档知识，不替代 MySQL 的价格、库存、订单、购物车等结构化查询。

## 已完成能力

### 1. DeepSeek 调用链路

- Agent 服务已切换为 DeepSeek 兼容的 Chat Completions 工具调用链路。
- 已处理 DeepSeek 工具调用中的 `reasoning_content` 限制，避免工具调用请求被 400 拒绝。
- 已修复多工具调用消息顺序，确保 assistant 的 `tool_calls` 后面紧跟对应 tool 结果。
- 已加强系统提示词：商品、价格、库存、订单、购物车等业务数据必须通过工具获取，不能让模型凭空编造。

### 2. Function Calling 工具

当前重点工具包括：

| 工具 | 作用 |
| --- | --- |
| `search_products` | 查询商品，支持名称、分类、价格区间、库存、发布时间过滤 |
| `get_product` | 根据商品 ID 查询商品详情 |
| `get_cart` | 查询当前用户购物车 |
| `add_to_cart` | 添加商品到购物车 |
| `update_cart_item` | 修改单个购物车条目数量 |
| `remove_cart_item` | 删除购物车条目 |
| `get_orders` | 查询订单列表 |
| `create_order` | 创建订单，需确认 |
| `cancel_order` | 取消订单，需确认 |

内部还支持批量购物车数量更新，用于处理“两个商品都改成 3 件”这类上下文指令。

### 3. 商品查询能力

- 已支持“4000 以内手机”“100-300 的充电宝”等价格筛选。
- 已支持“2026-07 之后发布的耳机”等发布时间筛选。
- 已补充一批更适合演示的商品数据，避免日期类问题查不到结果。
- 商品结果会写入上下文，后续可以通过“刚才那个”“红米手机”“OPPO 那个”等自然语言指代继续操作。

### 4. 购物车上下文能力

- 已支持从最近商品结果中解析商品并加入购物车。
- 已支持查看购物车后，对具体商品修改数量。
- 已支持“两个商品都改成 3 件”这类批量数量修改。
- 已支持“确认”“对”“可以”等自然语言确认后执行待确认动作。
- 已避免重复确认时反复询问但不执行的问题。

### 5. 本地启动

当前推荐一行命令启动完整本地链路：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-local-full.ps1
```

该脚本负责：

- 设置 UTF-8 控制台环境。
- 检查并启动 MySQL，必要时使用 Docker Compose 兜底。
- 初始化数据库结构和演示数据。
- 启动 Java 后端。
- 启动 Agent 服务。
- 做健康检查，启动完成后可直接访问本地网站测试。

停止命令：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\stop-local-full.ps1
```

## 下一阶段优先级

### P0：购物车与订单体验继续打磨

目标：让购物车相关操作更像真实电商客服，而不是反复靠模型猜。

待做事项：

- 前端展示购物车时，不再只显示 Markdown 表格，改成稳定的购物车条目卡片。
- 每个购物车条目提供明确操作：增加、减少、改数量、删除、下单。
- Agent 在修改购物车前必须明确锁定 `cart_id`，不能只靠商品名反复追问。
- 批量修改时，需要在确认卡里列出每个商品的旧数量和新数量。
- 确认执行失败时，展示真实失败原因，例如未登录、购物车条目不存在、库存不足。

验收用例：

```text
查看我的购物车
把红米手机改成 2 件
确认
把两个商品都改成 3 件
确认
删除 OPPO 那个
确认
```

### P0：确认流可靠性

目标：所有高风险动作只确认一次，确认后必须执行或给出明确失败原因。

待做事项：

- 确认卡增加动作类型、动作摘要、确认 ID、过期时间。
- 确认成功后立即清理 pending confirmation，避免重复执行。
- 用户说“确认修改”“对”“执行吧”时，优先消费当前 pending confirmation。
- pending confirmation 过期时，自动提示用户重新发起操作。
- 前端确认按钮如果拿到 401/403/404，展示中文业务错误，而不是 `invalid JSON`。

### P1：Function Calling 评测集

目标：用固定测试集防止后续改动把工具调用链路改坏。

建议新增评测：

| 场景 | 示例 |
| --- | --- |
| 商品价格筛选 | `4000以内手机` |
| 商品日期筛选 | `2026-07之后发布的耳机` |
| 商品区间筛选 | `价格100-300的充电宝` |
| 加入购物车 | `把刚才那个红米手机加入购物车` |
| 单项改数量 | `把红米手机改成2件` |
| 批量改数量 | `两个商品都改成3件` |
| 查看订单 | `查询我的订单` |
| 取消订单 | `取消刚才那个订单` |

验收标准：

- 结构化数据问题必须触发工具调用。
- 没有工具结果时不能输出商品、价格、库存、订单结论。
- 工具失败时必须展示可理解的失败原因。
- 多轮上下文中商品指代不能丢失。

### P1：前端工具调用可视化

目标：让面试和演示时能清楚看到 Agent 的真实工作过程。

待做事项：

- 工具调用折叠区显示工具名、入参、耗时、返回条数。
- 失败工具调用用红色状态展示原因。
- 成功工具调用用简洁状态展示，例如“已查询商品 6 条”“已更新购物车 2 项”。
- 普通用户默认折叠，演示时可展开。

### P1：数据与脚本维护

目标：保证每次启动后的演示数据一致。

待做事项：

- 固定 demo 用户、地址、商品、购物车、订单初始化数据。
- 增加 `reset-demo.ps1` 与 `start-local-full.ps1` 的职责边界说明。
- 为商品发布时间、库存、分类补充更多覆盖场景。
- 避免脚本重复插入相同演示数据。

### P2：RAG 模块接入

接入时机：Function Calling 的商品、购物车、订单链路稳定后再做。

RAG 只负责：

- 保修政策。
- 退换货规则。
- 售后流程。
- 平台 FAQ。
- 产品手册说明。

意图路由建议：

```text
结构化业务数据：商品、价格、库存、购物车、订单 -> Function Calling
文档知识：保修、退货、售后、规则、FAQ -> RAG
普通闲聊：直接大模型回答
```

不要把数据库全量数据塞进 prompt，也不要用向量库替代 MySQL 做价格、时间、库存筛选。

## 技术风险

### 1. 会话状态仍是内存态

如果 Agent 服务重启，pending confirmation、最近商品、购物车上下文会丢失。后续如果要做得更像生产项目，可以接 Redis 或数据库持久化。

### 2. 登录态可能和前端缓存有关

如果后端或 Agent 重启后购物车出现 401/403，优先重新登录 demo 用户，再测试购物车功能。

### 3. 模型仍可能不按预期调用工具

虽然提示词已加强，但后端仍应保留防护：只要识别为商品、价格、库存、订单、购物车意图，而模型没有 tool call，就应强制重试或走确定性工具分支。

## 推荐演示流程

```text
1. 商品推荐
   帮我找 4000 以内的手机

2. 上下文加入购物车
   把刚才那个红米手机加入购物车
   确认

3. 查看购物车
   查看我的购物车

4. 修改数量
   把红米手机改成 2 件
   确认

5. 批量修改
   两个商品都改成 3 件
   确认

6. 查询订单
   查询我的订单

7. 高风险动作确认
   取消最近那个订单
   确认
```

## 近期维护命令

启动完整本地环境：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\start-local-full.ps1
```

停止完整本地环境：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\stop-local-full.ps1
```

运行 Agent 测试：

```powershell
cd .\agent-service
.\.venv\Scripts\python.exe -m pytest tests/test_agent.py tests/test_conversation_state.py tests/test_api.py tests/test_ecommerce_client.py -q
```

Java 后端编译：

```powershell
.\mvnw.cmd -DskipTests compile
```

## 最终方向

这个项目接下来最值得继续强化的不是“让模型看起来更会聊天”，而是让它在真实业务动作上更可靠：

- 查商品必须查数据库。
- 改购物车必须拿到真实 cart item。
- 下单和取消订单必须经过确认。
- 工具失败必须可解释。
- RAG 只回答文档知识，不抢结构化数据的活。

等 Function Calling、上下文和确认流稳定后，再补 RAG、部署说明、演示视频和项目复盘文档，这样整个项目会更像一个真正能落地的企业级智能客服原型。
